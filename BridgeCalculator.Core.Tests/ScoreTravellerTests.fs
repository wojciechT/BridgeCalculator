namespace BridgeCalculator.Tests

// open System
// open Xunit

// open BridgeCalculator.Types
// open BridgeCalculator.ScoreTraveller

module ScoreTravellerTests = ()
//     [<Fact>]
//     let ``The same play should give 0 points`` () = 
//         let nsPair =  {Id = 1; PlayerNames = "North", "South"}
//         let ewPair =  {Id = 2; PlayerNames = "East", "West"}

//         let contract = {Declarer = North; DeclaredSuit = H; DeclaredTricks = 2; Vulnerable = false; Doubled = N }
//         let handResult = {DeclaredContract = contract; Tricks = 1}
//         let traveller = 
//             {
//                 BoardNumber = 1;
//                 Entries = [{NSPair = nsPair; EWPair = ewPair; Result = handResult}; {NSPair = nsPair; EWPair = ewPair; Result = handResult}]
//             }
        
//         let finalScore = 
//             score traveller
//             |> List.map(snd)

//         let expectedScore = [0;0;0;0]

//         Assert.Equal<Collections.Generic.IEnumerable<int>>(expectedScore, finalScore)
